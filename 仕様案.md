# Math Formula Game - MVP 仕様書（詳細版）

**作成日**: 2025-10-28  
**更新日**: 2025-10-28 08:32  
**対象**: MVP（Minimum Viable Product）  
**バージョン**: 2.1

---

## 1. プロジェクト概要

### 1.1 コンセプト
LaTeX/Markdown形式の数式記法を**楽しく学べる**Webアプリケーション。
**レンダリングされた数式を見て、それを再現するLaTeX記法を入力する**ことで、自然に記法を習得できる。

### 1.2 ターゲットユーザー
- **数式記法初心者**: LaTeX/Markdownを初めて学ぶ人
- **学生**: 数学や科学に苦手意識がある学生
- **研究者・エンジニア**: 論文やドキュメントで数式を書く必要がある人
- **一般読者**: 数式が登場する書籍やWeb記事への心理的ハードルを下げたい人

### 1.3 達成目標
- ✅ 数式記法に慣れる
- ✅ 数式が読める・書ける自信をつける
- ✅ 学習の心理的ハードルを下げる
- ✅ 実践的な記法を段階的に習得する
- ✅ **見た目の数式から記法を逆引きできる能力を養う**

---

## 2. 技術スタック

| 項目 | 技術 | 理由 |
|------|------|------|
| フロントエンド | React (JavaScript) | シンプルで調整しやすい |
| ビルドツール | Vite | 高速な開発環境 |
| 数式レンダリング | KaTeX | 軽量・高速 |
| シンタックスハイライト | 独自実装 | LaTeX記法の視覚化 |
| スタイリング | CSS + CSS Modules | シンプルで管理しやすい |
| 状態管理 | React Hooks (useState, useEffect) | 追加ライブラリ不要 |
| ローカルストレージ | localStorage API | 進捗保存 |
| ホスティング | GitHub Pages / Vercel | 無料で簡単にデプロイ |

---

## 3. 全体フロー

```
[スタート]
    ↓
[ホーム画面]
    ↓
[ステージ選択画面]
    ├─ ステージ1: 基本（アンロック済み）
    ├─ ステージ2: 分数・べき乗（ロック中）
    └─ ステージ3: 関数・記号（ロック中）
    ↓
[選択したステージの問題画面]
    ├─ 問題1 → 問題2 → ... → 問題5
    └─ リアルタイムプレビュー & ヒント機能
    ↓
[ステージクリア画面]
    ├─ スコア表示
    ├─ 次ステージのアンロック
    └─ 次ステージへ or ステージ選択に戻る
    ↓
[全ステージクリア画面]
    └─ おめでとう！もう一度プレイ
```

---

## 4. 画面構成（詳細版）

### 4.1 ホーム画面

```
┌─────────────────────────────────────────┐
│                                         │
│      📐 Math Formula Game 📐            │
│                                         │
│   LaTeX記法を楽しく学ぼう！              │
│                                         │
│                                         │
│          [ゲームスタート]                 │
│                                         │
│          [遊び方]  [設定]                │
│                                         │
│                                         │
│  進捗: ステージ 1/3 クリア               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                         │
└─────────────────────────────────────────┘
```

---

### 4.2 ステージ選択画面

```
┌─────────────────────────────────────────┐
│  ← 戻る        ステージ選択              │
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │ Stage 1     │  │ Stage 2     │      │
│  │ 基本の記法   │  │ 分数・べき乗 │      │
│  │             │  │             │      │
│  │   ✓ クリア  │  │   🔒 LOCKED │      │
│  │  ⭐⭐⭐      │  │             │      │
│  └─────────────┘  └─────────────┘      │
│                                         │
│  ┌─────────────┐                        │
│  │ Stage 3     │                        │
│  │ 関数・記号   │                        │
│  │             │                        │
│  │   🔒 LOCKED │                        │
│  │             │                        │
│  └─────────────┘                        │
│                                         │
└─────────────────────────────────────────┘
```

---

### 4.3 問題画面（通常時）- 新レイアウト

```
┌─────────────────────────────────────────┐
│  ← 戻る    Stage 1: 基本の記法     💡ヒント │
│                                         │
│  問題 1 / 5                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━         │
│                                         │
│  この数式をLaTeX記法で再現してください：  │
│                                         │
│  【問題】                                │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │           x²                      │ │  ← レンダリングされた
│  │                                   │ │     数式（大きく表示）
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【あなたの入力のプレビュー】              │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │           x²                      │ │  ← リアルタイム
│  │                                   │ │     プレビュー
│  │                                   │ │     （入力に応じて更新）
│  └───────────────────────────────────┘ │
│                                         │
│  【入力欄】                              │
│  ┌───────────────────────────────────┐ │
│  │ x^2                               │ │  ← 入力フィールド
│  └───────────────────────────────────┘ │  （シンタックスハイライト）
│                                         │
│          [回答を確認する]                │
│                                         │
│  正解: 0 / 0                            │
└─────────────────────────────────────────┘
```

**重要な変更点:**
1. **問題 = レンダリングされた数式**（日本語説明なし）
2. **レイアウト順序**: 問題（上）→ プレビュー（中）→ 入力（下）
3. 問題とプレビューを直接比較しやすい配置

---

### 4.4 問題画面（入力中）

```
┌─────────────────────────────────────────┐
│  ← 戻る    Stage 1: 基本の記法     💡ヒント │
│                                         │
│  問題 1 / 5                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━         │
│                                         │
│  この数式をLaTeX記法で再現してください：  │
│                                         │
│  【問題】                                │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │        x² + 2x + 1 = 0            │ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【あなたの入力のプレビュー】              │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │        x² + 2x                    │ │  ← 部分的に一致
│  │                                   │ │     （まだ入力中）
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【入力欄】                              │
│  ┌───────────────────────────────────┐ │
│  │ x^2 + 2x|                         │ │  ← カーソル位置
│  └───────────────────────────────────┘ │
│     ^   ← 緑色（上付き文字）            │
│                                         │
│          [回答を確認する]                │
│                                         │
│  正解: 0 / 0                            │
└─────────────────────────────────────────┘
```

---

### 4.5 ヒント表示時

```
┌─────────────────────────────────────────┐
│  ← 戻る    Stage 1: 基本の記法     💡ヒント │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ 💡 ヒント                         │ │
│  │                                   │ │
│  │ べき乗は ^ 記号を使います          │ │
│  │ 例: x^2 で「xの2乗」               │ │
│  │                                   │ │
│  │ 記法: 変数^{指数}                 │ │
│  └───────────────────────────────────┘ │
│                                         │
│  問題 1 / 5                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━         │
│                                         │
│  【問題】                                │
│  ┌───────────────────────────────────┐ │
│  │           x²                      │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【あなたの入力のプレビュー】              │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【入力欄】                              │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│          [回答を確認する]                │
└─────────────────────────────────────────┘
```

---

### 4.6 正解時

```
┌─────────────────────────────────────────┐
│  ← 戻る    Stage 1: 基本の記法           │
│                                         │
│  問題 1 / 5                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━         │
│                                         │
│  【問題】                                │
│  ┌───────────────────────────────────┐ │
│  │           x²                      │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【あなたの入力のプレビュー】              │
│  ┌───────────────────────────────────┐ │
│  │           x²                      │ │  ← 一致！
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ ✅ 正解！素晴らしい！              │ │  ← フィードバック
│  │                                   │ │     (緑色背景)
│  │ あなたの入力: x^2                 │ │
│  │                                   │ │
│  │ この記法を覚えました！             │ │
│  └───────────────────────────────────┘ │
│                                         │
│          [次の問題へ] (3秒後に自動遷移)   │
│                                         │
│  正解: 1 / 1      連続正解: 1           │
└─────────────────────────────────────────┘
```

---

### 4.7 不正解時

```
┌─────────────────────────────────────────┐
│  ← 戻る    Stage 1: 基本の記法           │
│                                         │
│  問題 1 / 5                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━         │
│                                         │
│  【問題】                                │
│  ┌───────────────────────────────────┐ │
│  │           x²                      │ │
│  └───────────────────────────────────┘ │
│                                         │
│  【あなたの入力のプレビュー】              │
│  ┌───────────────────────────────────┐ │
│  │           x2                      │ │  ← 異なる！
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ ❌ 惜しい！比較してみましょう       │ │  ← フィードバック
│  │                                   │ │     (赤色背景)
│  │ あなたの入力: x2                  │ │
│  │ → レンダリング: x2 (べき乗にならない) │
│  │                                   │ │
│  │ 正解例: x^2                       │ │
│  │ → レンダリング: x² (べき乗)        │ │
│  │                                   │ │
│  │ 💡 ^ 記号を使うとべき乗になります  │ │
│  └───────────────────────────────────┘ │
│                                         │
│          [もう一度挑戦]                  │
│          [正解を見て次へ]                │
│                                         │
│  正解: 0 / 1                            │
└─────────────────────────────────────────┘
```

---

### 4.8 レスポンシブデザイン（スマホ版）

```
┌───────────────────┐
│ ← Stage 1   💡   │
│                   │
│ 問題 1/5          │
│ ━━━━━━━━━━━━━    │
│                   │
│ 【問題】           │
│ ┌───────────────┐ │
│ │               │ │
│ │      x²       │ │
│ │               │ │
│ └───────────────┘ │
│                   │
│ 【プレビュー】     │
│ ┌───────────────┐ │
│ │               │ │
│ │      x²       │ │
│ │               │ │
│ └───────────────┘ │
│                   │
│ 【入力】           │
│ ┌───────────────┐ │
│ │ x^2           │ │
│ └───────────────┘ │
│                   │
│  [回答を確認]     │
│                   │
│ 正解: 0/0         │
└───────────────────┘
```

**レスポンシブ対応のポイント:**
- スマホでは縦に長い画面を活用
- 各セクションを折りたたみ可能に（オプション）
- ヒントボタンはヘッダーに固定
- フォントサイズは画面サイズに応じて調整

---

### 4.9 ステージクリア画面

```
┌─────────────────────────────────────────┐
│                                         │
│  🎉 ステージ 1 クリア！                  │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │   正解数: 4 / 5                   │ │
│  │   正答率: 80%                     │ │
│  │                                   │ │
│  │   評価: ⭐⭐⭐                     │ │
│  │   (90%以上で⭐⭐⭐)                │ │
│  │                                   │ │
│  │   🔓 ステージ2 がアンロック！      │ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│          [次のステージへ]                │
│          [ステージ選択に戻る]             │
│                                         │
└─────────────────────────────────────────┘
```

---

### 4.10 全ステージクリア画面

```
┌─────────────────────────────────────────┐
│                                         │
│  🏆 おめでとうございます！               │
│     全ステージクリア！                   │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │                                   │ │
│  │   総正解数: 13 / 15               │ │
│  │   総正答率: 86.7%                 │ │
│  │                                   │ │
│  │   クリア済みステージ: 3/3          │ │
│  │   獲得スター: ⭐⭐⭐ ⭐⭐ ⭐⭐⭐    │ │
│  │                                   │ │
│  │   あなたはLaTeX記法マスター！      │ │
│  │                                   │ │
│  └───────────────────────────────────┘ │
│                                         │
│          [もう一度プレイ]                │
│          [ホームに戻る]                  │
│                                         │
└─────────────────────────────────────────┘
```

---

## 5. ステージ構造

### 5.1 ステージ一覧（MVP: 3ステージ）

| ステージ | タイトル | 問題数 | 難易度 | カバーする記法 |
|---------|---------|-------|-------|--------------|
| 1 | 基本の記法 | 5問 | ★☆☆ | 上付き・下付き、簡単な式 |
| 2 | 分数・べき乗 | 5問 | ★★☆ | 分数、複雑なべき乗 |
| 3 | 関数・記号 | 5問 | ★★★ | ルート、シグマ、積分 |

**アンロック条件**:
- ステージ1: 最初からアンロック
- ステージ2: ステージ1を50%以上の正答率でクリア
- ステージ3: ステージ2を50%以上の正答率でクリア

---

### 5.2 問題データ構造（改訂版）

```json
{
  "stages": [
    {
      "id": 1,
      "title": "基本の記法",
      "description": "上付き文字・下付き文字など基本的な記法を学びます",
      "difficulty": 1,
      "unlocked": true,
      "problems": [
        {
          "id": 1,
          "displayLatex": "x^2",
          "correctAnswer": "x^2",
          "acceptableAnswers": ["x^{2}"],
          "hint": "べき乗は ^ 記号を使います。例: x^2",
          "category": "superscript",
          "explanation": "^ の後に数字を書くと上付き文字（べき乗）になります"
        },
        {
          "id": 2,
          "displayLatex": "x_1",
          "correctAnswer": "x_1",
          "acceptableAnswers": ["x_{1}"],
          "hint": "下付き文字は _ 記号を使います",
          "category": "subscript",
          "explanation": "_ の後に数字を書くと下付き文字になります"
        },
        {
          "id": 3,
          "displayLatex": "x^{10}",
          "correctAnswer": "x^{10}",
          "acceptableAnswers": [],
          "hint": "2桁以上の数字は {} で囲む必要があります",
          "category": "superscript_braces",
          "explanation": "複数文字のべき乗は中括弧で囲みます: x^{10}"
        }
      ]
    }
  ]
}
```

**重要な変更点:**
- `displayLatex`: 問題として表示する数式（レンダリング用）
- `correctAnswer`: 正解となるLaTeX記法
- `acceptableAnswers`: 許容される別解（`x^2` と `x^{2}` など）

---

### 5.3 各ステージの問題例（改訂版）

#### **ステージ1: 基本の記法**

| 問題（表示） | 正解 | ヒント |
|------------|------|--------|
| x² | `x^2` | べき乗は ^ を使います |
| x₁ | `x_1` | 下付き文字は _ を使います |
| x² + y² = 1 | `x^2 + y^2 = 1` | 複数のべき乗を組み合わせます |
| aᵢ | `a_i` | 下付き文字に変数も使えます |
| x¹⁰ | `x^{10}` | 2桁以上は {} で囲みます |

#### **ステージ2: 分数・べき乗**

| 問題（表示） | 正解 | ヒント |
|------------|------|--------|
| ½ | `\frac{1}{2}` | 分数は \frac{分子}{分母} |
| a/b | `\frac{a}{b}` | 変数の分数も同じ |
| (x+1)/(y-1) | `\frac{x+1}{y-1}` | 複雑な式も {} 内に |
| x²ʸ | `x^{2y}` | べき乗部分が複雑な場合 |
| x²/y² | `\frac{x^2}{y^2}` | 分数とべき乗の組み合わせ |

#### **ステージ3: 関数・記号**

| 問題（表示） | 正解 | ヒント |
|------------|------|--------|
| √2 | `\sqrt{2}` | 平方根は \sqrt{} |
| √(x²+y²) | `\sqrt{x^2+y^2}` | ルートの中も記法が必要 |
| Σⁿᵢ₌₁ i | `\sum_{i=1}^{n} i` | シグマは \sum_{下}^{上} |
| ∫₀¹ x dx | `\int_{0}^{1} x \, dx` | 積分は \int_{下}^{上} |
| lim(x→∞) f(x) | `\lim_{x \to \infty} f(x)` | 極限は \lim_{式} |

---

## 6. シンタックスハイライト仕様

### 6.1 ハイライト対象

| 記法 | 色 | 例 |
|-----|----|----|
| コマンド (`\\`) | `#0066cc` (青) | `\frac`, `\sum` |
| 上付き (`^`) | `#00aa00` (緑) | `x^2` |
| 下付き (`_`) | `#00aa00` (緑) | `x_1` |
| 中括弧 (`{}`) | `#ff8800` (オレンジ) | `x^{10}` |
| 数字 | `#aa00aa` (紫) | `123` |
| 変数 | `#333333` (通常) | `x`, `y` |

### 6.2 実装例

```javascript
// 簡易的なシンタックスハイライト実装
function highlightLatex(input) {
  return input
    .replace(/(\\[a-zA-Z]+)/g, '<span class="latex-command">$1</span>')
    .replace(/(\^)/g, '<span class="latex-superscript">$1</span>')
    .replace(/(_)/g, '<span class="latex-subscript">$1</span>')
    .replace(/([{}])/g, '<span class="latex-brace">$1</span>')
    .replace(/(\d+)/g, '<span class="latex-number">$1</span>');
}
```

```css
/* シンタックスハイライトのスタイル */
.latex-command { color: #0066cc; font-weight: bold; }
.latex-superscript { color: #00aa00; }
.latex-subscript { color: #00aa00; }
.latex-brace { color: #ff8800; }
.latex-number { color: #aa00aa; }
```

---

## 7. 判定ロジック（詳細版）

### 7.1 基本判定

```javascript
function checkAnswer(userInput, correctAnswer, acceptableAnswers = []) {
  try {
    // ユーザーの入力をレンダリング
    const userRendered = katex.renderToString(userInput, {
      throwOnError: false,
      output: 'html'
    });
    
    // 正解をレンダリング
    const correctRendered = katex.renderToString(correctAnswer, {
      throwOnError: false,
      output: 'html'
    });
    
    // HTML正規化して比較
    const normalizedUser = normalizeHTML(userRendered);
    const normalizedCorrect = normalizeHTML(correctRendered);
    
    // 完全一致チェック
    if (normalizedUser === normalizedCorrect) {
      return { correct: true, method: 'exact' };
    }
    
    // 許容される別解をチェック
    for (const acceptable of acceptableAnswers) {
      const acceptableRendered = katex.renderToString(acceptable, {
        throwOnError: false,
        output: 'html'
      });
      const normalizedAcceptable = normalizeHTML(acceptableRendered);
      
      if (normalizedUser === normalizedAcceptable) {
        return { correct: true, method: 'acceptable' };
      }
    }
    
    return { correct: false };
  } catch (error) {
    return { correct: false, error: true };
  }
}

function normalizeHTML(html) {
  return html
    .replace(/\s+/g, '') // 空白を削除
    .replace(/<!--.*?-->/g, '') // コメントを削除
    .toLowerCase(); // 小文字に統一
}
```

### 7.2 エラーハンドリング

```javascript
function validateInput(input) {
  if (!input || input.trim() === '') {
    return { 
      valid: false, 
      message: '入力してください' 
    };
  }
  
  try {
    katex.renderToString(input, { throwOnError: true });
    return { valid: true };
  } catch (error) {
    return { 
      valid: false, 
      message: 'LaTeX形式が正しくありません',
      detail: error.message
    };
  }
}
```

---

## 8. データ管理

### 8.1 ローカルストレージ

```javascript
// 保存するデータ構造
const gameProgress = {
  version: '1.0',
  stages: [
    {
      id: 1,
      unlocked: true,
      cleared: true,
      bestScore: 5,
      totalProblems: 5,
      stars: 3,
      attempts: 1
    },
    {
      id: 2,
      unlocked: true,
      cleared: false,
      bestScore: 0,
      totalProblems: 5,
      stars: 0,
      attempts: 0
    }
  ],
  totalCorrect: 5,
  totalProblems: 5,
  lastPlayedStage: 1,
  lastPlayedDate: '2025-10-28'
};

// 保存
function saveProgress(progress) {
  localStorage.setItem('mathGameProgress', JSON.stringify(progress));
}

// 読み込み
function loadProgress() {
  const saved = localStorage.getItem('mathGameProgress');
  return saved ? JSON.parse(saved) : getDefaultProgress();
}

// デフォルト進捗データ
function getDefaultProgress() {
  return {
    version: '1.0',
    stages: [
      { id: 1, unlocked: true, cleared: false, bestScore: 0, totalProblems: 5, stars: 0, attempts: 0 },
      { id: 2, unlocked: false, cleared: false, bestScore: 0, totalProblems: 5, stars: 0, attempts: 0 },
      { id: 3, unlocked: false, cleared: false, bestScore: 0, totalProblems: 5, stars: 0, attempts: 0 }
    ],
    totalCorrect: 0,
    totalProblems: 0,
    lastPlayedStage: null,
    lastPlayedDate: null
  };
}
```

---

## 9. コンポーネント構成（詳細版）

```
src/
├── components/
│   ├── pages/
│   │   ├── HomePage.jsx              // ホーム画面
│   │   ├── StageSelectPage.jsx       // ステージ選択画面
│   │   ├── GamePage.jsx              // ゲーム画面（メイン）
│   │   ├── StageClearPage.jsx        // ステージクリア画面
│   │   └── GameCompletePage.jsx      // 全クリア画面
│   │
│   ├── game/
│   │   ├── ProblemDisplay.jsx        // 問題表示（レンダリング済み数式）
│   │   ├── PreviewDisplay.jsx        // プレビュー表示
│   │   ├── LatexInput.jsx            // 入力欄（ハイライト付き）
│   │   ├── HintButton.jsx            // ヒントボタン
│   │   ├── HintDisplay.jsx           // ヒント表示
│   │   ├── FeedbackDisplay.jsx       // 正誤フィードバック
│   │   ├── ComparisonView.jsx        // 比較表示（不正解時）
│   │   └── ProgressBar.jsx           // 進捗バー
│   │
│   ├── common/
│   │   ├── Button.jsx                // 汎用ボタン
│   │   ├── Card.jsx                  // カードコンポーネント
│   │   ├── Header.jsx                // ヘッダー
│   │   ├── MathRenderer.jsx          // KaTeX レンダリング
│   │   └── ScoreDisplay.jsx          // スコア表示
│   │
│   └── stage/
│       ├── StageCard.jsx             // ステージカード
│       └── StageProgress.jsx         // ステージ進捗表示
│
├── utils/
│   ├── latexChecker.js               // LaTeX判定ロジック
│   ├── syntaxHighlight.js            // シンタックスハイライト
│   ├── storageManager.js             // ローカルストレージ管理
│   └── stageManager.js               // ステージ管理ロジック
│
├── data/
│   └── stages.json                   // 問題データ
│
├── hooks/
│   ├── useGameState.js               // ゲーム状態管理
│   ├── useProgress.js                // 進捗管理
│   └── useResponsive.js              // レスポンシブ対応
│
├── styles/
│   ├── common.css                    // 共通スタイル
│   ├── game.css                      // ゲーム画面スタイル
│   ├── syntax-highlight.css          // シンタックスハイライト
│   └── responsive.css                // レスポンシブスタイル
│
├── App.jsx                           // ルーティング
├── App.css
└── main.jsx
```

---

## 10. レスポンシブデザインの指針

### 10.1 ブレークポイント

```css
/* モバイル（デフォルト） */
@media (max-width: 767px) {
  .problem-display { font-size: 24px; }
  .preview-display { font-size: 24px; }
  .input-field { font-size: 18px; }
}

/* タブレット */
@media (min-width: 768px) and (max-width: 1023px) {
  .problem-display { font-size: 32px; }
  .preview-display { font-size: 32px; }
  .input-field { font-size: 20px; }
}

/* デスクトップ */
@media (min-width: 1024px) {
  .problem-display { font-size: 48px; }
  .preview-display { font-size: 48px; }
  .input-field { font-size: 24px; }
}
```

### 10.2 レイアウト調整

- **モバイル**: 縦長レイアウト、各要素を縦に並べる
- **タブレット**: 少し余白を持たせた縦長レイアウト
- **デスクトップ**: 中央寄せ、最大幅を設定（800px程度）

---

## 11. 開発ステップ（詳細版）

### **Phase 1: 環境セットアップ** (1日)
- [ ] Vite + React プロジェクト作成
- [ ] KaTeX インストール (`npm install katex react-katex`)
- [ ] ディレクトリ構造作成
- [ ] Git リポジトリ初期化

### **Phase 2: データ準備** (半日)
- [ ] `stages.json` 作成（3ステージ、各5問）
- [ ] データローダー実装 (`problemLoader.js`)
- [ ] ローカルストレージ管理 (`storageManager.js`)

### **Phase 3: 基本UI実装** (2日)
- [ ] ホーム画面 (`HomePage.jsx`)
- [ ] ステージ選択画面 (`StageSelectPage.jsx`)
- [ ] ゲーム画面の基本レイアウト (`GamePage.jsx`)
  - 問題表示（上）
  - プレビュー（中）
  - 入力欄（下）
- [ ] 基本的なCSS

### **Phase 4: ゲームロジック実装** (2日)
- [ ] 問題表示機能（KaTeXレンダリング）
- [ ] 入力機能（シンタックスハイライトなし）
- [ ] リアルタイムプレビュー (`PreviewDisplay.jsx`)
- [ ] 判定ロジック (`latexChecker.js`)
- [ ] 正誤フィードバック
- [ ] 比較表示機能（不正解時）

### **Phase 5: ステージ管理** (1日)
- [ ] ステージ進行ロジック
- [ ] アンロック機能
- [ ] 進捗保存・読み込み
- [ ] ステージクリア画面

### **Phase 6: UI/UX強化** (1-2日)
- [ ] シンタックスハイライト実装
- [ ] ヒント機能
- [ ] アニメーション追加
- [ ] レスポンシブ対応（モバイル・タブレット）

### **Phase 7: テスト＆調整** (1日)
- [ ] 全機能の動作確認
- [ ] 各デバイスでの表示確認
- [ ] バグ修正
- [ ] UI/UX調整
- [ ] パフォーマンス最適化

### **Phase 8: デプロイ** (半日)
- [ ] ビルド設定
- [ ] GitHub Pages または Vercel にデプロイ
- [ ] README更新

**合計見積もり**: 約8-10日

---

## 12. 今後の拡張案（MVP後）

### 短期（v2.0）
- [ ] タイマーモード
- [ ] 練習モード（間違えても次へ進める）
- [ ] 詳細な統計機能（正答率グラフ）
- [ ] 音声・効果音
- [ ] ダークモード

### 中期（v3.0）
- [ ] より多くのステージ（10ステージ以上）
- [ ] カスタム問題セット
- [ ] ランキング機能（ローカル）
- [ ] 実績・バッジシステム
- [ ] 多言語対応（英語など）

### 長期（v4.0以降）
- [ ] バックエンド追加（Node.js + DB）
- [ ] ユーザー登録・ログイン
- [ ] オンラインランキング
- [ ] ユーザー作成問題の共有
- [ ] SNS連携
- [ ] AI による個別最適化学習

---

## 13. 重要な技術メモ

### KaTeX の基本的な使い方

```javascript
import katex from 'katex';
import 'katex/dist/katex.min.css';

// 基本的なレンダリング
const html = katex.renderToString('x^2', {
  throwOnError: false,
  displayMode: false
});

// Reactコンポーネントで使用
import { InlineMath, BlockMath } from 'react-katex';

function MathDisplay({ latex, block = false }) {
  return block ? (
    <BlockMath math={latex} />
  ) : (
    <InlineMath math={latex} />
  );
}
```

### レスポンシブ数式表示

```javascript
// 画面サイズに応じて数式のサイズを調整
function ResponsiveMath({ latex }) {
  const [fontSize, setFontSize] = useState('2em');
  
  useEffect(() => {
    const updateSize = () => {
      if (window.innerWidth < 768) {
        setFontSize('1.5em'); // モバイル
      } else if (window.innerWidth < 1024) {
        setFontSize('2em'); // タブレット
      } else {
        setFontSize('3em'); // デスクトップ
      }
    };
    
    updateSize();
    window.addEventListener('resize', updateSize);
    return () => window.removeEventListener('resize', updateSize);
  }, []);
  
  return (
    <div style={{ fontSize }}>
      <InlineMath math={latex} />
    </div>
  );
}
```

---

## 14. 参考リンク

- [KaTeX Documentation](https://katex.org/docs/api.html)
- [KaTeX Supported Functions](https://katex.org/docs/supported.html)
- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [localStorage API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)
- [CSS Media Queries](https://developer.mozilla.org/en-US/docs/Web/CSS/Media_Queries)

---

**END OF DOCUMENT**
